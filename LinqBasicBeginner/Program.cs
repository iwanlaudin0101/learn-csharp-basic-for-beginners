using System;
using LinqBasicBeginner;

internal class Program
{
    private static void Main(string[] args)
    {

        // var StudentService = new StudentService();

        // var studentGroupMax = StudentService.QueryGroupMax();

        // foreach (var item in studentGroupMax)
        // {
        //     Console.WriteLine($"| {item.Level} \t | \t {item.HighestScore}\t|");
        // }

        // var studentHightScore = StudentService.queryHighScores(1, 85);
        
        // foreach (var item in studentHightScore)
        // {
        //     Console.WriteLine($"| {item.StudentId} \t | \t {item.Name} \t | \t {item.Year} \t | \t {item.Score} \t|");
        // }


        // var movies = new MovieService();

        // var movieContains = movies.MovieFilterContain("ave");
        // var movieDuration = movies.MovieDuration(140);

        // foreach (var item in movieDuration)
        // {
        //     Console.WriteLine($"| {item.Title} \t | \t {item.Director} \t | \t {item.Rating} \t | \t {item.Duration} \t|");
        // }

        // var movieCast = new MovieCastService();

        // var allData = movieCast.GetAll();
        // var allDataByMovieId = movieCast.GetMovieCastByMovieId(1);

        // foreach (var item in allDataByMovieId)
        // {
        //     Console.WriteLine($"| {item.Title} \t | \t {item.Director} \t | \t {item.ActorName} \t | \t {item.CharacterName}");
        // }
    }
}